const arr = [1,2,3]; //배열 리터러

arr.forEach(console.log)

for(let i = 0; i < arr.length ; i++) {
    console.log(arr[i]);
}

// 언어 :  Java, html, css, js, SQL(oracle DB)

// jQuery, servlet/JSP = 라이브러리

// spring framework

10 + 20 //리터럴만으로 요구하는 식만 적혀있다
var a = 10;
a;

